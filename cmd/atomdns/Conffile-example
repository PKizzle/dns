{
    debug
    dns {
        addr [::]:1053
        limits {
            tcp -1
            run numcpu()*3
        }
    }
    doh {
        addr [::]:10053
        limits {
            run numcpu()*1
        }
    }
    tls manual {
        cert handlers/global/testdata/test_cert.pem handlers/global/testdata/test_key.pem
    }
}

atoom.net {
    log
    dbfile atoom.net
}
